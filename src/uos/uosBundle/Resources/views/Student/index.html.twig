{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Student list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Studentid</th>
                <th>Firstname</th>
                <th>Lastname</th>
                <th>Gender</th>
                <th>Deptname</th>
                <th>Year</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('student_show', { 'id': entity.id }) }}">{{ entity.studentid }}</a></td>
                <td>{{ entity.firstname }}</td>
                <td>{{ entity.lastname }}</td>
                <td>{{ entity.gender }}</td>
                <td>{{ entity.deptName }}</td>
                <td>{{ entity.year }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('student_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('student_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('student_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
