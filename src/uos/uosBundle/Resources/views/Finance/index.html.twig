{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Finance list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Studentno</th>
                <th>Balance</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('finance_show', { 'id': entity.id }) }}">{{ entity.studentno }}</a></td>
                <td>{{ entity.balance }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('finance_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('finance_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('finance_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
